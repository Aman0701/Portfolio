export default class userModel{

    constructor(name,email,contact,transaction){
        this.name = name;
        this.email = email;
        this.contact = contact;
        this.transaction = transaction;
    }
}